PORT=3000
# Local: una URL. Produccion (Render): URL(s) publicas del frontend, separadas por coma.
# Ejemplo Render: CLIENT_URL=https://tu-juego.netlify.app
CLIENT_URL=http://localhost:5173
# Si es true, solo se aceptan orígenes listados en CLIENT_URL (sin excepción *.netlify.app).
# STRICT_CORS=false
